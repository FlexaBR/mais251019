type Article {
    id: Int
    name: String!
    description: String!
    image: String
    category_Id: Int
    usuario_id: Int
    
}

input ArticleInput {
    name: String!
    description: String!
    image: String
    category_id: Int
    usuario_id: Int    
}

input ArticleFiltro {
    id: Int
}