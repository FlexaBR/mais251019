# Pontos de entrada da sua API!
type Query {
    login(dados: UsuarioLoginInput!): Usuario
    usuarios: [Usuario]
    usuario(filtro: UsuarioFiltro!): Usuario
    perfis: [Perfil]
    perfil(filtro: PerfilFiltro!): Perfil

    articles: [Article]
    article(filtro: ArticleFiltro!): Article
    categories: [Category]
    category(filtro: CategoryFiltro!): Category
    faturas: [Fatura]
    pedidos: [Pedido]
    pedido(filtro: PedidoFiltro!): Pedido
    
}